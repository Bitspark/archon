{
  "name": "@bitspark/archon-cli",
  "version": "0.6.0",
  "description": "archon CLI: key / keygen / sign / verify over the identity floor.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Bitspark/archon"
  },
  "type": "module",
  "bin": {
    "archon": "dist/src/main.js"
  },
  "files": [
    "dist/src"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@bitspark/archon": "file:../../core/ts",
    "@bitspark/archon-sdk": "file:../../sdk/ts",
    "@noble/ciphers": "^2.4.0",
    "@noble/hashes": "^2.4.0"
  },
  "devDependencies": {
    "typescript": "^5.7",
    "@types/node": "^22"
  }
}
